# GCP Project Configuration
GCP_PROJECT_ID=your-project-id

# Cloud Tasks Configuration
CLOUD_TASK_LOCATION=asia-northeast1
CLOUD_TASK_QUEUE_NAME=runner-manager

# VM Lifecycle Configuration
VM_INACTIVE_MINUTES=3

# Cloud Run Service URL (set this to your Cloud Run service URL in production)
CLOUD_RUN_SERVICE_URL=http://localhost:8080

# Shared Secret for Authentication
# Used for both GitHub webhook verification and runner control endpoints (/runner/start, /runner/stop)
# Generate a secure random string and configure the same value in GitHub webhook settings
RUNNER_MANAGER_SECRET=your-secret-here

# GitHub App Configuration
# Required for checking running/queued jobs before stopping VM
GITHUB_APP_ID=123456
# Note: GITHUB_APP_PRIVATE_KEY is loaded from Secret Manager in production
# For local development, set this to the GitHub App private key (multi-line PEM format)
GITHUB_APP_PRIVATE_KEY="your-private-key-pem-content"
GITHUB_INSTALLATION_ID=12345678

# Runner Configuration (JSON array)
# Each entry maps a repository + labels to a specific VM instance
# Format: [{"repo": "owner/repo", "labels": ["self-hosted"], "vm_instance_name": "...", "vm_instance_zone": "..."}]
#
# Examples:
#   Single repository:
#     RUNNER_CONFIG='[{"repo": "owner/repo1", "labels": ["self-hosted"], "vm_instance_name": "github-runner-1", "vm_instance_zone": "asia-northeast1-a"}]'
#
#   Multiple repositories with different runners:
#     RUNNER_CONFIG='[{"repo": "owner/repo1", "labels": ["self-hosted", "linux"], "vm_instance_name": "github-runner-linux", "vm_instance_zone": "asia-northeast1-a"}, {"repo": "owner/repo2", "labels": ["self-hosted", "gpu"], "vm_instance_name": "github-runner-gpu", "vm_instance_zone": "us-central1-a"}]'
#
RUNNER_CONFIG='[{"repo": "owner/repo", "labels": ["self-hosted"], "vm_instance_name": "github-runner", "vm_instance_zone": "asia-northeast1-a"}]'
