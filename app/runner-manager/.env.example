# GCP Project Configuration
GCP_PROJECT_ID=your-project-id
GCP_ZONE=asia-northeast1-a
GCP_LOCATION=asia-northeast1

# VM Instance Configuration
VM_INSTANCE_NAME=github-runner

# Cloud Tasks Configuration
QUEUE_NAME=runner-controller

# Cloud Run Service URL (set this to your Cloud Run service URL in production)
SERVICE_URL=http://localhost:8080

# GitHub Webhook Secret
# Generate a secure random string and configure the same value in GitHub webhook settings
GITHUB_WEBHOOK_SECRET=your-webhook-secret-here

# VM Auto-stop Configuration (optional, default: 15 minutes)
INACTIVE_MINUTES=15

# Target labels for runner filtering (optional, default: "self-hosted")
# Comma-separated list of labels that must ALL be present in the job
# Examples:
#   TARGET_LABELS=self-hosted              # Any self-hosted job
#   TARGET_LABELS=self-hosted,linux        # Only linux self-hosted jobs
#   TARGET_LABELS=self-hosted,linux,gpu    # Only GPU-enabled linux self-hosted jobs
TARGET_LABELS=self-hosted
